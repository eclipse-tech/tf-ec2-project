# app/.env.example
# Copy to .env and fill in your values
# Then run: docker compose up -d

DB_HOST=host.docker.internal
DB_PORT=5432
DB_NAME=appdb
DB_USER=appuser
DB_PASS=Change_Me_Strong_P@ssw0rd!
AWS_REGION=ap-south-1
